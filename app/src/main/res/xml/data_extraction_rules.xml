<?xml version="1.0" encoding="utf-8"?>
<!--
  Data-extraction rules (Android 12+, android:dataExtractionRules).

  Same policy as backup_rules.xml (Android <= 11): exclude the entire app data directory
  from cloud backup and device-to-device transfer. The Room database carries sync state
  (envelopes, outbox, sync cursor) and DataStore carries the session — both are
  device-scoped and must never be restored onto another device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
